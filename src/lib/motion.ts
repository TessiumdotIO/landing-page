// Import specific exports from framer-motion instead of using "export *"
import {
  motion,
  AnimatePresence,
  useAnimation,
  useMotionValue,
  useTransform,
  useScroll,
  useInView,
  // Add other specific imports you need
} from 'framer-motion';

// Re-export them as named exports
export {
  motion,
  AnimatePresence,
  useAnimation,
  useMotionValue,
  useTransform,
  useScroll,
  useInView,
  // Add other specific exports you need
};
